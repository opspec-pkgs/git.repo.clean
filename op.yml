name: github.com/opspec-pkgs/git.clean
description: removes ignored files & dirs from a git repo
inputs:
  srcDir:
    dir:
      default: .
      description: directory containing the projects source code
  opts:
    string:
      default: -ffXd
outputs:
  srcDir:
    dir:
      description: directory containing the projects source code
version: 0.0.5
run:
  container:
    cmd:
      - sh
      - -ce
      - |
        echo 'determining files to clean'
        filesToClean=$(git clean -n $(opts)| sed 's/Would remove *//')
        
        echo "taking ownership of files"
        echo $filesToClean | xargs chown -R root:root
        # for some reason requires 2nd run on some hosts
        echo $filesToClean | xargs chown -R root:root
        
        echo 'removing files/dirs'
        echo $filesToClean | xargs rm -Rf
    dirs:
      /src: srcDir
    image: { ref: 'opspecpkgs/git.clean:0.0.5' }
    workDir: /src
